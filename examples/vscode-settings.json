{
  "$comment": [
    "Merge these into .vscode/settings.json in each BC extension repo.",
    "Do not overwrite the file.",
    "",
    "The AL analyzers are REQUIRED. The framework's instructions deliberately",
    "skip everything these catch, so agent context is spent on what a linter",
    "cannot see. Without them you lose half the coverage."
  ],

  "al.codeAnalyzers": [
    "${CodeCop}",
    "${UICop}",
    "${AppSourceCop}",
    "${PerTenantExtensionCop}"
  ],
  "al.enableCodeAnalysis": true,

  "$comment_plugins": "Required for agent plugins. May be managed by your organisation.",
  "chat.plugins.enabled": true,

  "$comment_marketplace": "Adds the marketplace for YOUR user profile across all workspaces.",
  "chat.plugins.marketplaces": [
    "<your-org>/al-bc-framework"
  ]
}
